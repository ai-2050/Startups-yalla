const Ping = () => {
  return (
    <div className="relative">
      <span className="bg-primary -top-2 -right-2 animate-ping w-3 h-3 absolute rounded-full" />
      <span className="bg-primary -top-2 -right-2 w-3 h-3 absolute rounded-full" />
    </div>
  );
};

export default Ping;
